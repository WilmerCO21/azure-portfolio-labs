# Lab 01 — VM + NSG + SSH + Nginx

## Goal
Deploy an Ubuntu VM, secure access with NSG (SSH restricted to my IP), install Nginx, and validate HTTP access.

## Evidence (Screenshots)
All screenshots are in: `./screenshots/`

- 02 — VM overview
- 03 — NSG inbound rules (SSH + HTTP)
- 04 — SSH connection
- 05 — Nginx welcome page
- 06 — Nginx service status + local curl check
